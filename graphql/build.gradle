plugins {
    id 'hexa-springboot-web'
    id 'org.springframework.boot'
}

base {
    archivesName = "graphql"
    version = globalVersion
}

dependencies {
    implementation libs.springboot.graphql
    testImplementation libs.springboot.graphql.test
}
